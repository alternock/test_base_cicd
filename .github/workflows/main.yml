name: test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    
jobs:
  eventos:
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar tipo de evento
        run: |
          echo "Este workflow se disparó por: ${{ github.event_name }}"
        
