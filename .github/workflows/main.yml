name: base

on: [push]

jobs:
  comandos:
    runs-on: ubuntu-latest
    steps:
      - run: whoami 
      - run: pwd
      - run: ls /
      - run: date
      - run: uname -a
      - run: |
          echo "este es un mensaje echo"
          ls -la
